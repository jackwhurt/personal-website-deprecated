version: 1
frontend:
  phases:
    # IMPORTANT - Please verify your build commands
    build:
      commands: ["npm build"]
  artifacts:
    # IMPORTANT - Please verify your build output directory
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: ["dist/amplify-app"]
